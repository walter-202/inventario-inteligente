/**
 * Application-wide constants.
 *
 * Centralizes magic strings and configuration values that were previously
 * scattered across the codebase as inline literals.
 */

/** Default password assigned to new collaborator accounts when no password is provided. */
export const DEFAULT_COLLABORATOR_PASSWORD = "Lidemoda2026!";

/** Locale used for date/time formatting (Bolivian Spanish). */
export const APP_LOCALE = "es-BO";

/** Locale used for currency/number formatting. */
export const CURRENCY_LOCALE = "es-ES";

/** Minimum password length required for collaborator accounts. */
export const MIN_PASSWORD_LENGTH = 6;
